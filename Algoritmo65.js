/*Algortimo 65: Volver a la cordura

Esta función debería devolver un objeto, pero no hace lo que pretende. ¿Qué ocurre?*/

const mystery = (obj) => {
  var results =
    {sanity: 'Hello'};
  return results;
}

console.log(mystery())